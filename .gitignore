node_modules/
dist/
.turbo/
*.tsbuildinfo

# Next.js build output (apps/admin). .next-dev is the dev server's own
# directory — see apps/admin/next.config.mjs for why they are separate.
.next/
.next-dev/
next-env.d.ts

# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Runtime
logs/
*.log
tmp/
data/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Locally downloaded speech models/binaries (see docs/telegram-setup.md)
.local/
